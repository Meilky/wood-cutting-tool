{
	"name": "wood-cutting-tool-ui",
	"version": "1.1.0",
	"description": "",
	"scripts": {
		"start": "parcel serve src/index.html -p 9000 --cache-dir .cache",
		"build": "rm -rf ./dist/* && npx parcel build src/index.html --dist-dir dist --cache-dir .cache",
		"lint": "eslint --ignore-path .eslintignore --ext .ts .",
		"lint:fix": "eslint --ignore-path .eslintignore --ext .ts . --fix",
		"format": "prettier --ignore-path .eslintignore --write \"src/**/*.+(ts)\"",
		"clean": "rm -rf ./dist ./.cache"
	},
	"author": "",
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"@typescript-eslint/eslint-plugin": "^5.10.1",
		"eslint": "^8.7.0",
		"eslint-config-prettier": "^8.3.0",
		"parcel": "^2.2.1",
		"postcss": "^8.4.7",
		"postcss-modules": "^4.3.1",
		"prettier": "^2.5.1",
		"typescript": "^4.5.4",
		"typescript-plugin-css-modules": "^3.4.0"
	}
}
